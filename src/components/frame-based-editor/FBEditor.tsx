import { FBFrameT } from "../../model/frame-based"

type FBEditorProps = {
	  frames: FBFrameT[];
}

const FBEditor = (props: FBEditorProps) => {
  return (
	<div>FBEditor</div>
  )
}

export default FBEditor